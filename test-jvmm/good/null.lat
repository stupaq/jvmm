int main() {
  string s = (string)null;
  printInt(length(s));
  printInt(length((string)null));
  printInt(length(""));
  printInt(length("safd"));
  printInt(length(nuller()));
  if (null == null)
    printString("OK");
  if (nuller() == (string)null)
    printString("OK");
  return 0;
}

string nuller() {
  return (string)null;
}

int length(string s) {
  if (s != (string)null) {
    return s.length;
  } else {
    return -1;
  }
}
